git ignore에 등록된 폴더입니다.
이폴더(git_ignore_folder)에 있는 파일들은 깃에 올라가지 않습니다.
저는 테스트용도의 컨트롤러 서비스 파일을 추가했습니다.

git ignore이 적용이 안되면 캐시삭제를 해주세요

명령어
// 캐시를 모두 삭제
git rm -r --cached .

// .gitignore에 입력된 파일 목록을 제외한 다른 모든 파일을 다시 트래킹
git add .

// 커밋
git commit -m "clear git cache"